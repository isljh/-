# 专家模型增量更新智能体：目录结构草案

## 文档说明

本文用于展示“专家模型增量更新智能体”未来可能采用的大致目录结构，仅作为个人留存和后续讨论参考。

这不是必须立即创建的完整模板，也不是不可修改的行业标准。实际目录应随着契约实现、工具开发、Pi 接入和真实模型更新任务逐步形成，避免提前建立大量空目录。

整体上可以分为三个部分：

1. 智能体系统仓库；
2. 单次模型更新任务目录；
3. 独立的目标模型项目。

---

## 一、智能体系统仓库

智能体系统仓库保存架构、产品规格、Agent 定义、工具实现、流程编排、测试和外部 Harness 适配代码。

```text
智能体/
├── AGENTS.md                         # Agent 入口、全局边界和阅读导航
├── README.md                         # 项目定位、当前状态和文档入口
│
├── docs/                             # 设计与项目记录
│   ├── architecture.md               # 总体架构和角色分工
│   │
│   ├── product-specs/                # 产品规格
│   │   └── minimal-model-update-agent.md
│   │
│   ├── design-docs/                  # 专题设计
│   │   ├── harness-engineering-principles.md
│   │   ├── tools.md                  # 工具设计，需要时建立
│   │   ├── runtime-and-artifacts.md  # 运行和产物管理，需要时建立
│   │   └── decisions/                # 重要设计决定
│   │       └── 0001-pi-prototype-harness.md
│   │
│   ├── exec-plans/                   # 构建智能体系统的执行计划
│   │   ├── active/                   # 正在执行的计划
│   │   └── completed/                # 已完成的计划
│   │
│   ├── evaluations/                  # 系统验收和评估规则
│   └── references/                   # 外部框架和研究资料
│       └── pi-agent-loop.md
│
├── schemas/                          # 机器可读的契约定义
│   ├── task-input.*
│   ├── update-plan.*
│   ├── execution-result.*
│   └── review-result.*
│
├── src/                              # 智能体系统核心代码
│   └── expert_model_update/
│       ├── agents/                   # 三个 Agent 的角色配置
│       │   ├── decision/
│       │   ├── execution/
│       │   └── review/
│       │
│       ├── orchestration/            # 状态转换、批准和任务调度
│       ├── contracts/                # 契约读取、写入和校验
│       ├── state/                    # 任务状态管理
│       │
│       └── tools/                    # 与 Harness 无关的业务工具
│           ├── model_inspection/     # 模型信息和结构分析
│           ├── training/             # 训练启动和运行控制
│           ├── evaluation/           # 评估和指标比较
│           ├── checkpoints/          # 权重保存、校验、加载验证
│           ├── artifacts/            # 日志、配置和产物清单
│           └── methods/              # 增量学习方法组件
│               └── ewc/
│
├── skills/                           # 提供给 Agent 的方法知识
│   └── ewc/
│       ├── SKILL.md                  # 原理、条件、步骤和验收标准
│       └── references/               # 必要参考资料
│
├── integrations/                     # 外部系统适配层
│   └── pi/
│       ├── roles/                    # Pi 中三个 Session 的角色配置
│       ├── extensions/               # 将业务工具注册给 Pi
│       └── session-configs/          # Session 配置，需要时建立
│
├── tests/                            # 自动化测试
│   ├── contracts/                    # 四类契约测试
│   ├── tools/                        # 工具测试
│   ├── orchestration/                # 状态和批准流程测试
│   └── end_to_end/                   # 最小闭环测试
│
├── scripts/                          # 开发和运维辅助命令
│   ├── validate_contracts.*
│   ├── verify_checkpoint.*
│   └── build_review_packet.*
│
└── examples/                         # 小型示例，不保存正式任务产物
    └── minimal-task/
```

### 主要目录的职责

| 目录 | 主要职责 |
| --- | --- |
| `docs/` | 保存架构、产品规格、设计决定、执行计划和参考资料 |
| `schemas/` | 保存可以由程序校验的任务与交接契约 |
| `src/` | 保存 Agent、流程、状态管理和业务工具的实现 |
| `skills/` | 保存供 Agent 阅读的方法知识和操作指导 |
| `integrations/` | 保存 Pi 等外部系统的适配层，不重复实现业务算法 |
| `tests/` | 验证契约、工具、状态转换和完整闭环 |
| `scripts/` | 保存面向开发、检查和交付的辅助命令 |
| `examples/` | 保存小型示例，不存放正式任务产物 |

---

## 二、单次模型更新任务目录

单次模型更新任务目录保存一次真实任务的输入、计划、运行记录、训练产物、审阅结果和最终交付物。

由于模型权重、训练日志和数据可能较大，这个目录可以位于智能体系统仓库之外。仓库中只需保存能够定位这些产物的引用和完整性信息。

```text
model-update-tasks/
└── <task-id>/
    ├── task-input.*                  # 人类提交的任务输入
    ├── task-state.*                  # 当前状态和状态转换记录
    │
    ├── plans/
    │   ├── update-plan-v1.*
    │   └── update-plan-v2.*          # 重规划产生新版本，不覆盖旧版本
    │
    ├── workspace/
    │   ├── target-project/           # 隔离后的目标模型项目
    │   └── workspace-metadata.*      # 项目版本和环境信息
    │
    ├── runs/
    │   ├── <validation-run-id>/      # 小规模验证
    │   │   ├── config.*
    │   │   ├── command-record.*
    │   │   ├── logs/
    │   │   ├── metrics/
    │   │   └── artifacts.*
    │   │
    │   └── <training-run-id>/        # 正式训练
    │       ├── config.*
    │       ├── command-record.*
    │       ├── logs/
    │       ├── checkpoints/
    │       ├── metrics/
    │       └── artifacts.*
    │
    ├── execution/
    │   └── execution-result.*
    │
    ├── reviews/
    │   └── review-result.*
    │
    └── deliverables/
        ├── model-weights.*            # 或指向外部权重存储
        ├── checksum.*
        ├── metric-comparison.*
        ├── model-record.*
        └── review-packet.*
```

### 任务目录的基本原则

1. 每个任务使用稳定且唯一的 `task-id`。
2. 每次验证、训练和评估使用独立的 `run-id`。
3. 计划重规划时创建新版本，不覆盖旧计划。
4. 失败和重试保留原始命令、退出状态与日志。
5. 原模型权重只读，不被新模型权重覆盖。
6. 大型文件可以保存在外部，但必须留下稳定引用和校验信息。
7. 尚未确定保留策略前，不自动删除权重、日志和评估证据。

---

## 三、目标模型项目保持独立

目标模型项目不是智能体系统源代码的一部分。智能体系统读取任务和计划，在隔离工作区中操作目标项目，并把运行证据和最终产物写入任务目录。

```text
智能体系统仓库
    ↓ 读取任务输入和已批准计划
隔离的目标模型项目
    ↓ 修改、验证、训练和评估
单次任务运行目录
    ↓ 保存权重、日志、指标和审阅证据
最终交付包
```

应避免把外部目标模型项目直接复制进智能体系统仓库并长期混合维护。必要时可以使用独立工作区或 Git worktree，但代码隔离不等于数据、环境、日志、权重和 GPU 资源已经隔离。

---

## 四、工具、Skill 与 Pi Extension 的位置

三者解决的问题不同：

| 类型 | 建议位置 | 作用 |
| --- | --- | --- |
| 业务工具 | `src/expert_model_update/tools/` | 执行模型分析、训练、评估、权重检查等确定性动作 |
| Skill | `skills/` | 告诉 Agent 某种方法何时适用、需要什么前提、如何实施和验收 |
| Pi Extension | `integrations/pi/extensions/` | 将业务工具注册给 Pi，处理 Harness 接入 |

例如，EWC 可以拆分为：

```text
skills/ewc/
└── SKILL.md
```

其中说明 EWC 的原理、适用条件、必要输入、实施步骤和验收标准。

实际算法组件则放在：

```text
src/expert_model_update/tools/methods/ewc/
├── fisher.*
├── penalty.*
└── validation.*
```

Pi 接入代码只负责调用这些业务组件：

```text
integrations/pi/extensions/
└── ewc-tools.*
```

这样可以保证核心工具不依赖 Pi。未来即使更换 Harness，也可以继续复用同一套业务能力。

---

## 五、目录设计原则

1. **根据真实需求逐步增长。** 没有内容时不提前创建空目录。
2. **入口简短。** `AGENTS.md` 和 `README.md` 负责导航，不承载全部细节。
3. **业务能力与 Harness 解耦。** 核心工具不能只能从 Pi 中运行。
4. **按能力分类工具。** 优先按模型分析、训练、评估和产物管理分类，不按 MLP、CNN、Transformer 等模型类型建立一键改造工具。
5. **建设计划与模型更新计划分开。** `docs/exec-plans/` 记录智能体系统的建设工作；`UpdatePlan` 属于具体模型更新任务。
6. **源代码与运行产物分开。** 训练日志、权重和指标不与系统实现代码混在一起。
7. **结构一旦稳定就进行机械化约束。** 后续通过 Schema、测试和检查器保证目录、契约和产物之间的一致性。

---

## 六、建议的渐进建设顺序

无需一次创建完整结构，可以按以下顺序逐步增加：

1. 保持现有 `AGENTS.md`、`README.md` 和 `docs/`。
2. 确定契约序列化格式后建立 `schemas/`。
3. 开始实现最小程序时建立 `src/` 和 `tests/`。
4. 开始实现 EWC 方法时建立相应的 `skills/ewc/` 和 `tools/methods/ewc/`。
5. 接入 Pi 时建立 `integrations/pi/`。
6. 启动第一次真实模型更新时建立受管理的任务运行目录。
7. 出现重复检查需求后再建立 `scripts/`、CI 和更完整的评估目录。

目录结构服务于系统目标，可以随着实践调整；但重要调整应记录原因，避免同一概念在多个位置重复出现。
