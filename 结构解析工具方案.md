# 项目理解 Agent 与模型安全实例化流程

项目理解 Agent 不应该自己创建模型实例。它应该在形成一个或多个有证据支持的**模型创建方案**后停止。后续必须经过确定性的静态检查，再由专门工具创建实例。

推荐边界如下：

```text
TaskInput confirmed
        ↓
项目理解 Agent
        ↓
形成模型候选与模型创建方案
        ↓
静态验证工具
        ↓
确认方案信息是否完整、内部是否一致
        ↓
安全实例化工具
        ↓
得到真实模型实例并加载权重
        ↓
结构解析工具
        ↓
更新决策 Agent
```

---

## 1. 第一阶段：项目理解 Agent

项目理解 Agent 负责理解源码语义并收集证据，但**不执行模型构造**。

它应该输出类似：

```yaml
model_candidates:
  - candidate_id: primary_network
    construction_callable: utils.factory:get_model
    construction_call_site: trainer:_train
    runtime_extraction_path: $._network

    arguments:
      model_name:
        value: tagfex
        evidence: exps/tagfex.json:3

    lifecycle_operations:
      - operation: update_fc
        arguments:
          total_classes: unresolved
        evidence: trainer.py:104

    checkpoint:
      path: E:/models/tagfex.pkl
      load_target: $._network
      evidence: trainer.py:128

    confidence: medium

    unresolved:
      - total_classes
```

Agent 在这里停止。

它只能说：

> 根据源码，我认为可以按照这个方案创建模型。

不能说：

> 模型已经成功创建并且结构正确。

---

## 2. 第二阶段：静态验证工具

原有的静态分析代码仍然需要，而且应该放在**项目理解 Agent**和**安全实例化工具**之间。

该工具不依赖 Agent 的主观判断，而是负责对模型创建方案进行确定性的机械检查。

### 2.1 检查源码位置

通过 AST 等静态分析方式检查：

* 文件和模块是否存在；
* 函数、类或工厂是否存在；
* 调用点是否真的调用了目标工厂；
* `_network` 是否确实由该调用结果产生；
* 创建之后是否调用了 `update_fc` 等结构修改方法；
* 权重最终加载到了哪个对象。

例如，需要验证：

```text
trainer._train()
    ↓
utils.factory.get_model(...)
    ↓
self._network = ...
    ↓
self._network.update_fc(...)
    ↓
load checkpoint into self._network
```

静态工具的任务是证明 Agent 给出的创建方案确实能够在源码中找到对应证据，而不是仅依赖 Agent 的语义判断。

---

### 2.2 检查参数完整性

静态验证工具需要检查：

* 构造函数要求哪些参数；
* 创建方案是否提供这些参数；
* 参数来自配置文件、默认值还是源码推断；
* 是否仍存在未解决的必需参数；
* 配置文件中的值是否与实际调用链一致。

例如：

```yaml
arguments:
  model_name:
    value: tagfex
    source: config
    evidence: exps/tagfex.json:3

  pretrained:
    value: false
    source: default
    evidence: utils/factory.py:24

  total_classes:
    value: unresolved
    source: runtime
```

如果仍然存在无法确定的必需参数，则模型创建方案不能直接进入实例化阶段。

---

### 2.3 静态分析 checkpoint

在不创建模型实例的情况下，工具可以安全读取 checkpoint 的元信息，并检查：

* `state_dict` 中的参数键；
* 各参数张量的形状；
* 分类头输出维度；
* 输入通道数；
* 隐藏层维度；
* 特征维度；
* 是否存在 `module.`、`_network.` 等包装前缀；
* checkpoint 更可能对应哪个模型候选。

例如：

```yaml
checkpoint_findings:
  classifier.weight:
    shape: [100, 512]
    implication:
      num_classes: 100
      feature_dim: 512
```

这样，即使源码中的类别数原本来自运行时数据集，也可能通过 checkpoint 得到：

```yaml
total_classes: 100
```

因此，checkpoint 不仅用于后续加载权重，也可以作为静态验证阶段的重要证据来源。

---

### 2.4 判断方案是否可以进入实例化

静态验证工具最终应该输出明确状态。

验证通过时：

```yaml
status: ready_for_instantiation
```

例如：

```yaml
status: ready_for_instantiation

validated_candidate:
  candidate_id: primary_network

resolved_arguments:
  model_name: tagfex
  total_classes: 100

checkpoint_compatibility:
  status: compatible
```

如果仍然存在问题，则输出：

```yaml
status: incomplete

blocking_issues:
  - 无法确定模型构造需要的 backbone_name
  - 两个候选都与 checkpoint 形状相符
```

静态验证工具的作用不是创建模型实例，而是：

> 防止一个信息不完整、证据不足或内部存在矛盾的模型创建方案直接进入运行阶段。

---

## 3. 第三阶段：安全实例化工具

只有静态验证通过以后，Agent 才允许调用专门的实例化工具。

Agent 的职责只是发起调用并读取结果：

```text
Agent：
请按照已验证的创建方案实例化 candidate_1。

        ↓

安全实例化工具：
执行实际创建并返回结果。
```

真正执行模型构造的是工具，而不是 Agent。

安全实例化工具负责：

1. 建立隔离环境；
2. 设置 CPU 模式；
3. 限制文件写入；
4. 禁止或拦截数据下载；
5. 导入目标模块；
6. 调用模型工厂或构造函数；
7. 执行必要的模型生命周期操作；
8. 从包装对象中取得真正的模型；
9. 加载 checkpoint；
10. 返回模型句柄或结构分析结果。

例如：

```text
validated construction plan
        ↓
sandbox / isolated runtime
        ↓
import target module
        ↓
call get_model(...)
        ↓
execute update_fc(...)
        ↓
extract $._network
        ↓
load checkpoint
        ↓
return model handle
```

因此，不应该让 Agent 通过临时编写 Python 代码的方式自行：

```python
from utils.factory import get_model

model = get_model(...)
model.update_fc(...)
model.load_state_dict(...)
```

这种方式的问题在于：

* 实例化过程没有统一的安全约束；
* 无法统一限制网络访问；
* 无法统一限制文件写入；
* 缺乏标准日志；
* 难以保证过程可重复；
* Agent 可以绕过预先验证的创建方案；
* 不利于后续审计。

因此，模型实例化必须集中到一个专门的、受控的工具中完成。

---

## 4. 第四阶段：结构解析工具

安全实例化工具得到真实模型以后，再把模型实例交给原有的结构分析核心。

结构解析工具负责分析：

* 模块层级；
* 参数名称；
* 参数形状；
* 可训练参数；
* 分类头；
* backbone；
* 模型大小；
* 可能被冻结的区域；
* 与增量更新有关的结构特征。

例如可以分析：

```python
model.named_modules()
model.named_parameters()
model.state_dict()
```

并形成结构事实：

```yaml
model_structure:
  backbone:
    type: ResNet
    feature_dim: 512

  classifier:
    path: fc
    in_features: 512
    out_features: 100

  trainable_parameters:
    count: 11234567
```

这些结构事实随后提供给更新决策 Agent，用于判断模型适合采用何种增量更新方法。

---

## 5. 两类“静态分析”需要明确区分

这里实际上存在两个容易混淆的“静态分析”概念。

| 分析类型                   | 是否需要模型实例 |
| ---------------------- | -------- |
| 项目源码静态分析               | 不需要      |
| checkpoint 张量和键分析      | 通常不需要    |
| 对 `nn.Module` 实例进行结构遍历 | 需要       |

前两类属于真正意义上的**实例化前分析**。

例如：

```text
AST
配置文件
源码调用链
checkpoint state_dict
```

这些都不要求模型已经被创建。

而原工具中针对：

```python
named_modules()
named_parameters()
```

等内容进行的分析，虽然：

* 不执行训练；
* 不进行 forward；
* 不修改模型；

但它所分析的对象已经是一个真实的 `nn.Module` 实例。

因此更准确地说，这部分应该叫：

> **模型实例结构解析**

而不是：

> **项目源码静态分析**

---

## 6. 更准确的职责划分

整个流程可以划分为五个明确角色。

### 6.1 项目理解 Agent

负责：

> 找到“模型可能如何创建”。

主要工作：

* 理解源码语义；
* 识别模型候选；
* 找到模型工厂或构造函数；
* 找到创建调用点；
* 找到运行时模型对象；
* 找到必要生命周期操作；
* 找到 checkpoint；
* 收集参数及其证据。

不负责：

* 创建模型实例；
* 验证模型一定可以运行；
* 判断模型结构一定正确。

---

### 6.2 静态验证工具

负责：

> 检查“这个模型创建方案是否有源码和权重依据”。

主要工作：

* AST 验证；
* 调用链验证；
* 参数完整性检查；
* checkpoint 元信息分析；
* 候选与 checkpoint 匹配；
* 解决部分可静态推导参数；
* 判断方案是否可以进入实例化。

不负责：

* 真正调用模型构造函数。

---

### 6.3 安全实例化工具

负责：

> 严格按照已经验证的创建方案创建模型实例。

主要工作：

* 建立受控运行环境；
* 导入源码；
* 调用模型工厂；
* 执行生命周期操作；
* 提取目标模型；
* 加载 checkpoint；
* 返回实例化结果。

不负责：

* 自己重新决定模型应该怎么创建；
* 决定使用 ER、EWC、LoRA 等增量方法。

---

### 6.4 结构解析工具

负责：

> 从真实模型实例中提取结构事实。

主要工作：

* 遍历模块；
* 遍历参数；
* 识别 backbone；
* 识别 classifier；
* 分析冻结状态；
* 提取参数规模；
* 提取增量更新相关结构能力。

---

### 6.5 更新决策 Agent

负责：

> 根据已经确认的模型事实决定如何更新模型。

例如：

```text
CNN
    ↓
Replay / EWC / KD

ViT
    ↓
Prompt / Adapter / LoRA

预训练大模型
    ↓
Adapter / LoRA / Prototype
```

它消费的是结构解析工具返回的确定性事实，而不是重新猜测模型结构。

---

## 7. 最终推荐流程

最终流程建议统一为：

```text
TaskInput confirmed
        ↓
项目理解 Agent
        │
        │  输出：
        │  model_candidates
        │  construction_plan
        │  evidence
        │  unresolved
        ↓
静态验证工具
        │
        ├─ AST / 调用链验证
        ├─ 参数完整性验证
        ├─ checkpoint 静态分析
        └─ 候选一致性检查
        ↓
status = ready_for_instantiation
        ↓
安全实例化工具
        │
        ├─ 隔离环境
        ├─ 导入模块
        ├─ 创建模型
        ├─ lifecycle operations
        ├─ 提取真实模型
        └─ 加载 checkpoint
        ↓
真实模型实例
        ↓
结构解析工具
        │
        ├─ named_modules
        ├─ named_parameters
        ├─ backbone
        ├─ classifier
        └─ trainable regions
        ↓
ModelStructureReport
        ↓
更新决策 Agent
        ↓
增量更新方案
```

---

## 8. 核心原则

最终确定的原则应该是：

> **项目理解 Agent 在提交模型创建方案后停止，不能直接自行执行模型创建。模型创建方案必须首先经过确定性的静态验证，随后只能由受控的安全实例化工具执行。**

原有的三个参数，例如：

```text
model_definition_site
model_instantiation_site
model_checkpoint_path
```

仍然可以保留，但它们不再代表“已经确认的模型信息”，而应该作为**模型创建方案中的候选信息和证据**。

更准确地说：

```text
Agent
    ↓
发现候选信息

静态验证工具
    ↓
证明这些信息彼此一致

安全实例化工具
    ↓
证明该方案实际能够创建并恢复模型

结构解析工具
    ↓
证明该实例具有怎样的真实模型结构
```

也就是说，四个阶段回答的是四个不同的问题：

```text
项目理解 Agent：
“源码看起来应该怎么创建模型？”

静态验证工具：
“这个创建方案在源码和 checkpoint 层面是否成立？”

安全实例化工具：
“这个方案实际上能不能成功创建并恢复模型？”

结构解析工具：
“成功创建出来的模型真实结构是什么？”
```

这样以后，Agent 的语义理解、静态程序分析、运行时模型创建和模型结构解析之间的边界就会比较清晰，也更适合后续扩展成通用的多项目模型解析与增量更新框架。
